---
import { Icon } from "astro-icon/components";

interface Props {
  name: string;
  class?: string;
}

const { name, class: className = "w-4 h-4 inline-block mr-2" } = Astro.props;
---

<Icon name={name} class={className} />

